# API

Interactive OpenAPI documentation is available at `/docs`. Authenticate with `POST /auth/login`, then send `Authorization: Bearer <access_token>` to protected endpoints. Main resources: `/agents`, `/workflows`, `/executions`, `/analytics/overview`, and `/recommendations`.
