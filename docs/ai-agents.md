# AI agents

Agents are organization-scoped configurations with instructions, selected model, status, and JSON configuration. The demo provider performs deterministic analysis over supplied revenue rows and provides an inspectable plan plus tool summary. Replace `MockProvider` with a production provider implementation without changing route contracts.
