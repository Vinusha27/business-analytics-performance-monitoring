# SQLite is the zero-configuration default. PostgreSQL URLs also work with a compatible driver.
DATABASE_URL=sqlite:///data/nexaretail.db
MONTHLY_REVENUE_TARGET=180000
COMPLETION_RATE_TARGET=0.94
GROSS_MARGIN_TARGET=0.35
RETURN_RATE_MAX=0.06
CANCELLATION_RATE_MAX=0.05
AOV_TARGET=85
